pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo 'build'
        sh 'yarn'
      }
    }

    stage('deploy') {
      steps {
        echo 'deploy'
      }
    }

  }
}